<div class="container">
	<div class="columns eight form_container">
<p id="notice"><%= notice %></p>
<p>
  <strong>From:</strong>
	<% user = User.find(@message.sender) %>
	<%= link_to(user_path(user)) do %>
  	<%= user.name %>
	<% end %>
</p>
<p>
  <strong>Subject:</strong>
  <%= @message.subject %>
</p>

<p>
  <%= @message.content %>
</p>
<%=link_to "Back", :back, :class => "button" %>
<%= link_to 'Reply', message_path(@message.sender), :class => "button"%>


</div>
</div>
